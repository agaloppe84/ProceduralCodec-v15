# configs/requirements.docs.txt
-r requirements.dev.txt   # ← mets ici le nom exact de TON fichier existant
mkdocs>=1.5
mkdocs-material
mkdocstrings[python]
pymdown-extensions
# mike  (optionnel si tu veux versionner les docs)
